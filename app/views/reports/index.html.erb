<table>
  <tr>
    <th>Project</th>
    <th>Time</th>
  </tr>
  <% @report.each do |project| %>
    <tr>
      <td><%= project.name %></td>
      <td><%= distance_of_time_in_words(project.time) %></td>
    </tr>
  <% end %>
</table>
