<h1>New Container</h1>
<%= form_for @container do |form| %>
  <div class="form-group">
    <%= form.label :path %>
    <%= form.select(:path, @container_paths.zip(@container_paths)) %>
  </div>
  <div class="form-group">
    <%= form.submit 'Save' %>
  </div>
<% end %>
