<h1>New Container</h1>
<%= form_for @container do |form| %>
  <div class="form-group">
    <%= form.label :url %>
    <%= form.select(:url, Container.possible_urls.map { |url| [url[7..-1], url] }) %>
  </div>
  <div class="form-group">
    <%= form.submit 'Save' %>
  </div>
<% end %>
