<h1>New Container</h1>
<%= form_for @container do |form| %>
  <div class="form-group">
    <%= form.label :path %>
    <%= form.select(:path, [Container.possible_paths, Container.possible_paths]) %>
  </div>
  <div class="form-group">
    <%= form.submit 'Save' %>
  </div>
<% end %>
